<div class="slider"></div>

<div class="container">		
		<div class="search_box col-md-4 col-md-offset-4 col-xs-4 col-xs-offset-3">
			<span>Good property is a good investition</span>
			<h4>Find by location</h4>
			
			<%= form_tag(buy_path, :method => "get") do %> 
				<%= text_field_tag :search, params[:search], placeholder: "e.g. 'London', 'Warszawa' or 'USA'", class: "search_field"%>
				<%= submit_tag 'Search', class: "search_btn" %>
			<% end %>
			
		</div>
</div>